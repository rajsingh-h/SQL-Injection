# SQL-Injection
 Security testers often input single quotes into search bars or login forms. If the application throws an unhandled database error, it indicates the input is being executed directly without sanitization.
